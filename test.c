#include <stdio.h>
#include <stdlib.h>

/* function to add variables */
int fun_add(int a, int b)
{
    ans = 45; /* default answer*/
    return ans;
}

int fun_sub(int a, int b)
{
    int ans = a > b ? a-b:b-a;
    if(ans > 100)
        return ans;
    else
        return 250;
}

int main()
{
    printf("\n Hello World");
    printf("\n Should we end ??");
    return 0;
}
